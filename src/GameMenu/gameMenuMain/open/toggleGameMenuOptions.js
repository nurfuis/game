import { BrowserWindow } from "electron";

let isMenuOpen = false;
let modalWindow;

export function toggleGameMenuOptions(mainWindow) {
  console.log("[main] Enable the the menu api.");
  // check configuration

  // then disable the menu once it is closed
}
